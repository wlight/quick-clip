# Changelog

本项目版本记录遵循 [Keep a Changelog](https://keepachangelog.com/) 风格。

## [1.3.4] — 2026-09-16

### 修复

- 点到面板外面面板不消失：旧逻辑只依赖窗口 `Deactivated`——面板没抢到前台时（前台锁 / 钩子唤起）窗口从未激活过，点到别的窗口根本不会有事件，表现为面板一直挂在屏幕上、只能再按一次 `Win + V`；热键唤起后 1.5s 宽限内的点击也被一律忽略。现按 Win11 剪贴板历史的做法改为**鼠标按下即判定**：`HotkeyService` 新增 `WH_MOUSE_LL` 低级鼠标钩子，鼠标按在本进程窗口之外立刻收起面板（与窗口是否激活无关）；另有 120ms 前台窗口看门狗兜底，补齐钩子失效或非点击式的前台切换。托盘点按与点外收起按 320ms 合并，避免面板刚收起又被托盘点按打开

## [1.3.3] — 2026-09-16

### 修复

- 顶部搜索行没有余量：420 宽的窗口里搜索框占掉约 262px，右侧筛选下拉与置顶按钮被挤在一起；占位文字偏长（含拼音提示）在窄窗口下还会溢出到相邻控件。现占位文字缩短为「搜索剪贴板内容」（拼音说明移入 ToolTip）、搜索框宽度限定 200 并左对齐（右侧留白，窄窗口自动收缩）、裁剪溢出内容、筛选下拉 82 → 74、控件间距 8 → 6

## [1.3.2] — 2026-09-15

### 变更

- 条目展示改为 Win11 剪贴板历史形态：由两列磁贴网格改为**单列宽卡片**自上而下堆叠，图片等比缩放限高（整图可见）、文本三行截断，序号 / 类型 / 时间压成一行元信息；悬停时卡片右上角浮出图钉、二维码、OCR 与「…」菜单，浮层底色跟随卡片状态（压在文字上不再糊成一片）。方向键（含 `←` / `→`）随单列布局改为逐项移动；`MainViewModel.MaxRenderedTiles` 更名 `MaxRenderedItems`

### 修复

- 面板在「焦点变化」时被误收起：确认框（清空列表）、另存为图片对话框、条目「…」菜单与悬停预览等自身窗口都会让主窗失去激活，旧逻辑一律按「失焦即隐」收起，表现就是点一下面板里的按钮、面板自己就没了。现统一收拢为 `HideIfFocusLost` 判定：置顶、浮层、菜单、预览、设置窗打开时不隐藏，**前台窗口属于本进程自身**时也不隐藏，只有焦点真正落到别的程序才收起；预览浮层关闭后补一次判定，避免面板滞留

## [1.3.1] — 2026-09-15

### 修复

- 搜索框描边错位：放大镜图标此前绝对定位叠在 `ui:TextBox` 上，与控件模板自带的边框、占位文字对不齐。现改由外层 `Border` 单独出框（聚焦时转强调色），内层文本框去边框、透明底，图标与输入框按列排布，任何 DPI / 缩放下都不会错位
- 两列磁贴布局下 `← / →` 无法移动选中项（此前只有 `↑ / ↓` 生效）：新增左右移动，同行内左右各移一格、行首行尾不折返；`↑ / ↓` 改为按实际列数跨行；焦点在搜索框且光标未抵边界时，`← / →` 仍归文本框移动光标

## [1.3.0] — 2026-09-15

### 变更

- 面板改为 Windows 11 剪贴板历史形态的浮动窗口：无标题栏，`Win + V` 时在鼠标位置浮出（默认光标右下，贴边自动翻转到左上，并钳制在光标所在显示器工作区内），淡入上浮出场 150ms，失焦即隐，Esc 关闭
- 条目由整行卡片改为等比磁贴网格（两列）：图片铺满磁贴、文本三行截断，选中走强调色描边、置顶走金色描边，角标显示 1~9 序号
- 新增条目「…」菜单：复制 / 粘贴到当前窗口 / 固定·取消固定 / 二维码 / OCR 识别文字 / 另存为图片 / 复制文件全路径 / 删除；底部命令栏集中状态、帮助、清空与设置，移除标题栏关闭按钮
- 方向键在网格中按行移动（每次两格），数字键 1~9 快速粘贴与其余快捷键不变

### 优化

- 磁贴网格不虚拟化，面板一次最多渲染 150 条（超出部分用搜索 / 筛选定位），避免一次解码过多缩略图拖慢浮出

### 修复

- 发布说明、README、Issue 模板与安装包信息里的仓库链接仍指向原作者仓库，改为当前仓库 `wlight/quick-clip`（更新检查早已指向当前仓库，链接与之一致）

## [1.2.11] — 2026-09-15

### 修复

- 开机自启动后不按 Win+V 就不记录复制内容：剪贴板监听此前挂在主窗口句柄上，而开机自启为静默驻留、主窗口不显示（窗口句柄未创建），监听要等用户手动唤起面板后才注册。现改为挂载在独立隐藏消息窗口上，进程启动即开始记录。

## [1.2.10] — 2026-09-09

### 变更

- 复制历史去重改为全历史永久去重：再次复制历史中已有的文本 / 链接 / 文件 / 图片时不再新增记录，而是刷新时间把该条目置顶；老库启动时自动补齐去重键（文本与文件按内容哈希，图片按 PNG 文件哈希）

## [1.2.9] — 2026-09-08

### 修复

- 焦点停留在搜索框时 `↑ / ↓` 无法移动列表选中项：WPF 文本框在 KeyDown 冒泡到窗口前会吞掉方向键，现改在 PreviewKeyDown 隧道阶段提前拦截，搜索框中方向键可正常选择条目，普通字符输入不受影响

## [1.2.8] — 2026-09-08

### 变更

- 个人自用分支：更新检查与下载源由原作者仓库切换为当前分支自己的 GitHub 仓库（wlight/quick-clip），不依赖原作者发布

## [1.2.7] — 2026-09-07

### 修复

- 开机自启后 Win+V 无法唤起面板：电脑重启后 Explorer 读取注册表释放了 Win+V，QuickClip 系统级 RegisterHotKey 成功生效，但底层键盘钩子为防误弹开始菜单必须吞掉 Win 键物理按下事件，导致系统热键引擎收不到完整和弦而不会派发 `WM_HOTKEY`；钩子此前误认为系统会处理从而跳过了唤起。现由钩子直接负责唤起并引入 300ms 线程安全防抖调度，彻底消除双端漏发死锁。

### 优化

- 开机自启动静默运行：识别 `--autostart` 启动参数，开机自启时保持静默驻留系统托盘待命，不主动弹出主窗口，避免开机干扰用户并杜绝登录阶段焦点抢占导致的状态混乱。

## [1.2.6] — 2026-09-04

### 新增

- 卡片列表文件条目新增「复制全路径」按钮：悬停文件卡片即可一键将该文件（或多个文件）的完整绝对路径以纯文本形式复制到系统剪贴板。

### 优化

- 全局纯文本粘贴快捷键（`Ctrl + Shift + V`）支持文件：当系统剪贴板中为复制的文件列表时，自动提取所有文件的纯文件名（换行分隔）作为纯文本粘贴，无需繁琐转换。

## [1.2.5] — 2026-09-02

### 新增

- 设置页 OCR 增加「离线模型（PP-OCRv6）」：一键下载 Small（约 30 MB）/ Medium（约 132 MB，推荐），或指定自备模型目录。展示名称、体积、官方同套评测精度；识别失败仍回退系统 OCR

### 变更

- OCR 的 Ollama 与 OpenAI 合并为「OpenAI 兼容」一项：同一组地址 / 模型 / 可选 API Key。旧设置会自动迁移
- 诊断日志改为每份 2MB、保留 2 份滚动备份（合计约 6MB），避免占满磁盘；下载与视觉 OCR 的易错步骤写入日志（不含密钥与图片）
- 离线模型设置去掉测试按钮，点选模型包后在下方显示当前启动模型及是否就绪

### 修复

- 离线 OCR 模型下载中状态会突然跳回「未下载」、进度条不走：失败/取消原因被界面刷新冲掉，进度条模板缺少 `PART_Track`。现保留上次失败原因，并记录 HTTP 状态、文件大小与校验结果
- `dotnet run` 后托盘在、Win+V 却唤不出：更新检查在后台线程保存时间戳并重注册热键，`RegisterHotKey` 返回 1408（窗口属于其他线程），随后 `Activated` 清掉失焦宽限，面板显示约 1 秒就被藏回托盘。热键改回 UI 线程注册，时间戳保存不再广播 Changed，唤起后宽限内保持置顶
- 离线 OCR 下载在 `ppocrv6_dict.txt` 误报「不是有效字典」：文件还在写入缓冲里就读盘校验。现先关闭流再校验，并为字典补上 SHA256
- 列表 OCR 按钮识别中不可重复点击，图标换成环状等待，完成后再恢复
- 设置页 OCR 引擎顺序改为系统自带 / 离线模型 / OpenAI 兼容；描述随当前选项变化，去掉卡片下多余提示

## [1.2.4] — 2026-08-28

### 新增

- 图片卡片新增「保存到本地」按钮（悬停图片条目，下载图标）：另存为 PNG / JPEG 到本地，把历史预览图导出为文件

### 变更

- 设置页去掉不可改的「唤起 / 隐藏面板」展示行；全局纯文本粘贴固定为 `Ctrl+Shift+V`，设置里只保留启用/禁用

### 修复

- 焦点在「管理员权限窗口」（如以管理员运行的终端）时按 `Win+V` 仍弹出系统剪贴板历史：低级键盘钩子受 Windows UIPI 权限隔离限制，收不到高权限窗口的按键。新增「系统剪贴板历史窗口守卫」：一旦发现系统剪贴板历史窗口弹出，立即注入 ESC 关闭并唤起 QuickClip 面板，保证任意窗口下 `Win+V` 都落到 QuickClip
- 修复 `RegisterHotKey` 成功时钩子与 `WM_HOTKEY` 双触发面板切换的隐患（钩子此时只吞键、不再重复触发）
- 部分场景截图从面板直接粘贴到聊天框失败（需先点「复制」再手动粘贴）：回填剪贴板现在同时写入 `CF_DIBV5` + `CF_DIB` + PNG 多种格式，兼容微信 / QQ / 钉钉等优先读取 DIBV5 的应用；粘贴前等待目标窗口真正成为前台再发送 Ctrl+V，避免偶发落错窗口

## [1.2.3] — 2026-08-20

### 修复

- 检查到新版本后界面一直停在「正在检查更新」，下载无进度、无日志。现立刻提示「发现 vX.Y.Z，正在下载」，设置页显示百分比，45 秒无数据视为失败并给出浏览器兜底

## [1.2.2] — 2026-08-19

### 修复

- RustDesk 等远程桌面用 `SendInput` 注入的 Win+V 被钩子当成自身重放按键直接放行，从而弹出系统剪贴板。现只忽略带 QuickClip 标记的注入，远程注入按真实 Win+V 接管

## [1.2.1] — 2026-08-19

### 修复

- 检查更新后自动下载安装包，点「立即更新」或下次手动启动时静默安装；下载失败才提供浏览器兜底，不再把跳转下载页当主路径
- 安装版升级后开机仍拉起旧绿色版（装完 1.2.0 重启变回 1.0.0）：安装包改写已有自启动路径、安装前结束 `QuickClip.exe`，安装版启动时接管失效/绿色版 Run 键，并结束占用同名互斥锁的其它路径进程
- 开机自启启动不再自动弹 UAC；已下载的更新改为托盘提示，点「立即更新」再装。手动启动仍会自动安装（24 小时内同一版本失败则改手动）
- 关机 / 安装器关闭请求不再被「关窗口即隐藏」拦住，避免锁住正在覆盖的 exe

## [1.2.0] — 2026-08-19

### 新增与优化

- **文件与路径双模式粘贴**：
  - 修复文件复制误判为纯文本的根本缺陷，优先识别 `CF_HDROP` 真实文件；
  - 默认按 `Enter` 粘贴真实文件对象（支持微信/QQ/桌面直接粘贴）；按 `Shift + Enter` 纯文本粘贴文件路径字符串；
  - 卡片主标题突出显示真实文件名，副标题精简展示大小，杜绝重复前缀与溢出截断。
- **视觉与美学重构**：
  - 移除条目前方竖色块，引入卡片全包围霓虹线条（未选中暗色、置顶金边、选中主题色高亮）；
  - 全面采用 Fluent 矢量图标，彻底去除 Emoji 字符依赖；
  - 新增 Windows 11 原生「Fluent Dark」暗黑主题，并优化微对比度与搜索框内凹层级感。
- **更新调度与自愈加固**：
  - 自动检查更新重构为冷启动 8s + 12 小时循环调度，支持 7 次重试熔断机制与失败直达链接；
  - 增加 35ms 系统前台焦点稳定缓冲，彻底根除重型软件下偶发粘贴丢键问题；
  - 支持二次启动直接激活唤起已有面板，监听 `TaskbarCreated` 实现 Explorer 重建自愈。

## [1.1.3] — 2026-08-15

### 修复

- 剪贴板偶发全局失效（Ctrl+C/V 无反应、列表复制按钮无效）：读写剪贴板从 WPF/OLE 改为原生 Win32 API（OpenClipboard 快速失败 + 短重试），不再因属主进程延迟渲染卡死而无限阻塞、锁死全系统剪贴板
- 剪贴板被占用时把占用进程名/PID 写入日志，捕获超时 5 秒自动跳过，便于定位卡剪贴板的进程

### 体验

- Win+V 重新打开面板时始终默认选中第 1 条（最近一条），不再恢复上次选中

## [1.1.2] — 2026-08-13

### 体验

- 已识别二维码图：列表只显示「已识别二维码」和原图，不再把解码正文叠在卡片上；悬停预览里单独展示解码内容
- 生成二维码弹层标题去掉「手机扫码接力」
- 生成/放大二维码不再把原文铺在码下面；解码正文只出现在已识别图片的预览解码区

### 修复

- 悬停预览解码绿块在文本/普通图片上也会出现

### 变更

- 绿色版不再自动覆盖 exe 并重启：下载完成后点「打开下载目录」，退出后再自行替换
- 历史只按条数上限淘汰（默认 233，置顶豁免），不再按 24 小时过期

## [1.1.1] — 2026-08-13

### 修复

- 检查更新经常 403/超时：超时放宽，失败后改走 GitHub Releases 页面；请求走系统代理和默认凭据

### 变更

- 去掉「以管理员身份重启」：日常剪贴板不需要管理员权限

## [1.1.0] — 2026-08-13

### 功能

- 列表二维码按钮复用：文本/链接悬停预览并生成码；已识别二维码图显示解析图标，点击复制解码文本
- 双渠道发布：绿色自包含 `QuickClip.exe` + 依赖运行时的 `QuickClip-Setup-win-x64.exe`
- 静默检查更新（启动约 90 秒，24 小时节流，可关）；下载到本地后一点「立即更新」
  - 安装版启动 Setup
  - 绿色版自动覆盖当前 exe 并重启

### 修复

- 剪贴板图片 DIB Alpha 全 0 时预览全透明、二维码无法识别；优先读 PNG，全透明图按不透明落盘，启动补修历史预览
- 悬停预览本地 Visibility 盖住绑定，导致已识别二维码图左侧预览空白
- OpenAI/Ollama 失败只写「识别失败」；现展示 HTTP 状态与响应摘要，回退系统前先修全透明预览

### 体验

- OCR 设置增加「测试接口」；结果弹窗标题显示实际引擎（离线识别 / ollama-模型 / 模型名），弹层随主窗口缩放可滚动
- 列表「？」快捷键提示与设置绑定一致，改键后即时刷新
- Win11 用系统圆角；Win10 用窗口区域裁成同样圆角（主列表 + 设置）

### 更新

- 默认开启自动检查，仅访问 GitHub Releases；更新包只从 GitHub HTTPS 下载

## [1.0.0] — 2026-08-12

### 功能

- `Win + V` 唤起面板（钩子接管，避免误弹开始菜单）
- 剪贴板历史：文本 / 图片 / 链接 / 文件，本地 SQLite
- 淘汰策略：最大条数（默认 233）+ 24 小时双限，置顶豁免
- 搜索：关键词 + 拼音首字母；列表筛选与清空（非置顶）
- 交互：单击选中、双击粘贴、卡片按钮复制 / 二维码悬停预览 / OCR / 置顶 / 删除
- 全局纯文本粘贴与面板快捷键（`Win+V`、`1~9` 固定，其余可配置）
- 二维码生成与图片内识别；OCR：系统 / 可选 Ollama、OpenAI
- 多主题（Terminal、One Dark、GitHub、Nord 等）
- 窗口置顶（`Ctrl+P`）：失焦不藏、粘贴后保持打开
- 捕获体积上限（超大文本/图片不记历史，文件只记路径）；不改写系统剪贴板
- 开机自启、托盘、检查更新；远程会话渲染降级

### 平台

- 支持：Windows 10 1809+、Windows 11（x64）
- 不支持：Windows 7 / 8

### 许可

- MIT
